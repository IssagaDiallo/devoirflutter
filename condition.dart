void main() {
  // Déclarez un nombre à vérifier
  int nombre = 5; 

  // Vérifiez si le nombre est positif, négatif ou zéro
  if (nombre > 0) {
    print("Le nombre est positif.");
  } else if (nombre < 0) {
    print("Le nombre est négatif.");
  } else {
    print("Le nombre est zéro.");
  }
}
